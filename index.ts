function printUserInfo(userInfo, productInfo) {
    console.log(userInfo.name);
    console.log(userInfo.age);
    console.log(userInfo.job)
}

const userInfo = {
    name: '',
    age: 11,
    job: ''
};
